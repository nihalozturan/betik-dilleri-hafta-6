def icerik_topla(urr,f_icerik)
    urr = 'https://www.milligazete.com.tr/arsiv/'
    r = requests.get(url)
    soup = BeautifulSoup(r.content, 'html.parser')
    date=soup.find("time",{"class":"fw-bold"})
    title=soup.find("h1").getText()
    content=''
    icerik=soup.find("div",{"class":"article-text container-padding"}).findall('p')
    for i in range(len(icerik)):
        content+=i.getText()
        son='{} ; {} ; {} ; {}'.format(urr,date,title,content)
        with open(f_icerik, "a", encoding="utf-8") as file:
            file.write(son.strip() + "\n")
